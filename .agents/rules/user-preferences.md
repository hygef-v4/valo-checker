# User Preference Rules

- **Git Commit Directive**: NEVER execute `git commit` automatically after editing code. ONLY run `git commit` when the user explicitly requests to commit (e.g. "commit", "giúp tôi commit").
